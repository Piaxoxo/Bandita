# Work imagery (homepage glass panels + "In frame" glimpse)

Drop the two food reference photos here with these exact names:

- food-01.jpg  (beetroot carpaccio with feta + rocket — the wider shot)
- food-02.jpg  (close-up of feta, beetroot, balsamic)

The homepage "In frame" section and the floating glass panels detect these
files at runtime and only render once both are present — so the build stays
clean if they are missing.

Note: the Pia-Alice vineyard photo (content creation for Hilton International
GmbH) is reserved for the ABOUT page — do NOT place it here.
